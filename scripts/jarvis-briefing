#!/usr/bin/env bash
# Atajo: briefing Jarvis sin depender de git pull completo
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec python3 "${REPO_ROOT}/scripts/jarvis_serve.py" briefing "$@"
